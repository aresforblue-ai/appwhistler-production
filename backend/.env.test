# Test Environment Configuration
# This file is used during test execution

NODE_ENV=test
PORT=5001

# Test Database (PostgreSQL)
# These are safe default values for local testing only - not connected to any production systems
# These values are used by automated tests in CI/CD environments
DB_HOST=localhost
DB_PORT=5432
DB_NAME=appwhistler_test
DB_USER=postgres
DB_PASSWORD=test_db_p@ssw0rd_2024
# NOTE: For CI, override this with a secret if possible.

# Test Authentication
JWT_SECRET=test_jwt_secret_key_for_testing_only
JWT_EXPIRES_IN=1h
REFRESH_TOKEN_SECRET=test_refresh_token_secret

# Test Redis
REDIS_URL=redis://localhost:6379

# Disable external services in tests
SENTRY_DSN=
SENDGRID_API_KEY=
HUGGINGFACE_API_KEY=
PINATA_API_KEY=
PINATA_SECRET_KEY=

# Test CORS
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5001
